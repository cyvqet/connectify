apiVersion: v1
kind: PersistentVolume
metadata:
  name: connectify-mysql-pv
spec:
  storageClassName: record
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/data"